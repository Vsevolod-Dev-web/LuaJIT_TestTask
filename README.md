# LuaJIT fix-task by Vsevolod Polyakov

## Задача: Исправвить ошибки, написать патч.

### Проблема

В файле lj_tab.c не установлена зависимость с заголовочным файлом `<limits.h>`, в котором инициалезированна константа `INT_MAX`, используемая в условном операторе, в файле lj_tab.c.

### Решение

    1. Переходим к файлу с ошибкой.
    2. Добавляем зависимость `#include <limits.h>`
    3. Собираем билд.
    4. Успешно.

Получаем сообщение `OK Successfully built LuaJIT`, что говорит об успешной
сборке. 

Провверим работает ли LuaJIT. Находясь в директории LuaJIT-2.1.0.-beta3/src
пытаемся вывести версию LuaJIT: `./luajit -v`

Получаем сообщение: LuaJIT 2.1.0-beta3 -- Copyright (C) 2005-2017 Mike Pall. http://luajit.org/
что значит, что все работает. 

### Создание патча

Находясь в корне проекта `~/LuaJIT_TestTask` 

Создаем патч, сравнивая исправленную версию с оригиналом
```bash
diff -u original_sources/LuaJIT-2.1.0-beta3/src/lj_tab.c \
        patched_sources/LuaJIT-2.1.0-beta3/src/lj_tab.c > patches/lj_tab_fix.patch
```

Патч готов.

### Диагностика

Далее, важный шаг. Необходимо провести небольшую диагностику, чтобы убедиться,
что патч не только компелируется, но и создаёт корректный LuaJIT.

1. Проверим способность запускать и выполнять простой код:

`./luajit -e "print('Тест пройден. LuaJIT исправен')"`

Получаем ответ: `Тест пройден. LuaJIT исправен.`

Успешно.

2. Проведём небольшой тест для таблиц, проверим файл lj_tab.c:

`./luajit -e "local t={}; for i=1,1000000 do t[i]=i end; print('Таблица на 1 млн элементов создана. Последний элемент:', t[1000000])"`

Получаем вывод: `Таблица на 1 млн элементов создана. Последний элемент: 1000000`

Успешно.


